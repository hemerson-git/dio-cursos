// assigning values ​​from an array to a variable

const arr = ['Apple', 'Banana', 'Orange'];
const apple = arr[0];
console.log(apple);

// Destructuring an array

const [apple2] = arr;

console.log(apple2);
